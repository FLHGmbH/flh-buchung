services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: buchung
      POSTGRES_PASSWORD: buchung
      POSTGRES_DB: buchung
    ports:
      - "5432:5432"
    volumes:
      - buchung_pg:/var/lib/postgresql/data

volumes:
  buchung_pg:
